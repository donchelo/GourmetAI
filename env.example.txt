# API de Gemini (Google AI Studio) - Requerido
REACT_APP_GEMINI_API_KEY=tu_api_key_de_gemini

# API de Anthropic (Claude) - Requerido para recetas
REACT_APP_ANTHROPIC_API_KEY=tu_api_key_de_anthropic

# Configuración del Servidor (Local)
PORT=3000
SERVER_PORT=3001
NODE_ENV=development

# URL de la API (opcional en local, predeterminado a http://localhost:3001)
# En producción (Vercel) no es necesario ya que se usa /api/* relativo
# REACT_APP_API_URL=http://localhost:3001
