# ============================================
# GourmetAI - Variables de Entorno
# ============================================
# Renombra este archivo a .env y configura tus valores

# === API de Gemini (Requerido) ===
REACT_APP_GEMINI_API_KEY=tu_api_key_de_gemini

# === Airtable - Historial (Opcional) ===
AIRTABLE_API_KEY=tu_api_key_airtable
AIRTABLE_BASE_ID=tu_base_id_airtable
AIRTABLE_TABLE_NAME=Generaciones

# === Servidor ===
PORT=3001
NODE_ENV=development

# === URLs Permitidas (produccion) ===
ALLOWED_ORIGINS=http://localhost:3000

# === ngrok (Opcional) ===
# NGROK_URL=https://tu-url.ngrok.io

